// script.js

// Optional: Add a special effect or sound for when the page loads
document.addEventListener('DOMContentLoaded', () => {
    console.log("Birthday page loaded, ready to celebrate!");
    // Example: Play a sound effect (requires an audio file)
    // let audio = new Audio('path/to/happy_birthday_song.mp3');
    // audio.play();
  });
  